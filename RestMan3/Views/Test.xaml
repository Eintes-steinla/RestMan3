<Window x:Class="RestMan3.Views.Test"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Test Database Connection" 
        Height="500" Width="700"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="🔌 Test Kết Nối Database" 
                   FontSize="24" 
                   FontWeight="Bold"
                   Foreground="#2196F3"
                   Margin="0,0,0,20"/>

        <!-- Connection Info -->
        <Border Grid.Row="1" 
                Background="White" 
                CornerRadius="8" 
                Padding="15"
                Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Thông tin kết nối:" 
                           FontWeight="Bold" 
                           FontSize="14"
                           Margin="0,0,0,10"/>
                <TextBlock x:Name="txtConnectionInfo" 
                           Text="Chưa kết nối..." 
                           FontSize="12"
                           Foreground="#666"
                           TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <!-- Test Results -->
        <Border Grid.Row="2" 
                Background="White" 
                CornerRadius="8" 
                Padding="15">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="txtResults" 
                           FontFamily="Consolas"
                           FontSize="12"
                           TextWrapping="Wrap"
                           Foreground="#333"/>
            </ScrollViewer>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center"
                    Margin="0,20,0,0">

            <Button x:Name="btnTestConnection" 
                    Content="🔌 Test Kết Nối"
                    Click="BtnTestConnection_Click"
                    Width="150" Height="40"
                    Margin="5"
                    Background="#4CAF50"
                    Foreground="White"
                    FontWeight="Bold"
                    Cursor="Hand"
                    BorderThickness="0"/>

            <Button x:Name="btnTestQuery" 
                    Content="📊 Test Query"
                    Click="BtnTestQuery_Click"
                    Width="150" Height="40"
                    Margin="5"
                    Background="#2196F3"
                    Foreground="White"
                    FontWeight="Bold"
                    Cursor="Hand"
                    BorderThickness="0"/>

            <Button x:Name="btnTestDapper" 
                    Content="⚡ Test Dapper"
                    Click="BtnTestDapper_Click"
                    Width="150" Height="40"
                    Margin="5"
                    Background="#FF9800"
                    Foreground="White"
                    FontWeight="Bold"
                    Cursor="Hand"
                    BorderThickness="0"/>

            <Button x:Name="btnClear" 
                    Content="🗑️ Clear"
                    Click="BtnClear_Click"
                    Width="100" Height="40"
                    Margin="5"
                    Background="#F44336"
                    Foreground="White"
                    FontWeight="Bold"
                    Cursor="Hand"
                    BorderThickness="0"/>
        </StackPanel>
    </Grid>
</Window>